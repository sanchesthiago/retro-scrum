[build]
builder = "dockerfile"

[deploy]
startCommand = "node combined-server.js"
healthCheckPath = "/health"
healthCheckTimeout = 100

[environments]
  [environments.production]
    NODE_ENV = "production"
    PORT = 3000
